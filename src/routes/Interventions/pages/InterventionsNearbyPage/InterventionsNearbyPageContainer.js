import { connect } from 'react-redux'
import InterventionsNearbyPageView from './InterventionsNearbyPageView'

const mapStateToProps = (state) => {
  return {}
}
const mapDispatchToProps = (dispatch) => {
  return {}
}
export default connect(mapStateToProps, mapDispatchToProps)(InterventionsNearbyPageView)