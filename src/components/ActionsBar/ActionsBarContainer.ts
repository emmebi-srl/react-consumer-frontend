import { connect } from 'react-redux'; 
import ActionsBarView from './ActionsBarView';

const mapStateToProps = (state) => {
  return { }
}; 
const mapDispatchToProps = (dispatch) => {
  return {}
};
export default connect(mapStateToProps, mapDispatchToProps)(ActionsBarView); 