import { connect } from 'react-redux'; 
import PrivateAreaView from './PrivateAreaView';

const mapStateToProps = () => ({}); 
const mapDispatchToProps = () => ({});
export default connect(mapStateToProps, mapDispatchToProps)(PrivateAreaView); 